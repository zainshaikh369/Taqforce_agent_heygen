import { AbstractVoiceChatImplementation, VoiceChatConfig } from './base';
import protobuf from 'protobufjs';
export interface WebSocketVoiceChatConfig extends VoiceChatConfig {
    webSocket: globalThis.WebSocket;
    audioRawFrame: protobuf.Type;
}
export declare class WebSocketVoiceChat extends AbstractVoiceChatImplementation<WebSocketVoiceChatConfig> {
    private audioContext;
    private webSocket;
    private scriptProcessor;
    private mediaStreamAudioSource;
    private mediaDevicesStream;
    private audioRawFrame;
    protected _startVoiceChat(voiceChatConfig: WebSocketVoiceChatConfig): Promise<void>;
    protected _stopVoiceChat(): Promise<void>;
}
